---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ragflow-service-config
data:
  {{- if or .Values.ragflow.service_conf .Values.ragflow.oa.enabled }}
  {{- $serviceConf := deepCopy (default (dict) .Values.ragflow.service_conf) }}
  {{- if .Values.ragflow.oa.enabled }}
  {{- $oa := .Values.ragflow.oa }}
  {{- $oaChannel := dict
        "type" "oauth2"
        "icon" $oa.icon
        "display_name" $oa.displayName
        "client_id" (required "ragflow.oa.clientId is required when OA login is enabled" $oa.clientId)
        "client_secret_env" "OA_CLIENT_SECRET"
        "userinfo_client_id_header" "X-CLIENT-ID"
        "authorization_url" (required "ragflow.oa.authorizationUrl is required when OA login is enabled" $oa.authorizationUrl)
        "token_url" (required "ragflow.oa.tokenUrl is required when OA login is enabled" $oa.tokenUrl)
        "userinfo_url" (required "ragflow.oa.userinfoUrl is required when OA login is enabled" $oa.userinfoUrl)
        "redirect_uri" (required "ragflow.oa.redirectUri is required when OA login is enabled" $oa.redirectUri)
        "scope" $oa.scope
        "request_timeout" $oa.requestTimeout
  }}
  {{- $oauth := deepCopy (default (dict) (get $serviceConf "oauth")) }}
  {{- $_ := set $oauth "oa" $oaChannel }}
  {{- $_ := set $serviceConf "oauth" $oauth }}
  {{- end }}
  local.service_conf.yaml: |
    {{- $serviceConf | toYaml | nindent 4 }}
  {{- end }}
  {{- with .Values.ragflow.llm_factories }}
  llm_factories.json: |
    {{- . | toPrettyJson | nindent 4 }}
  {{- end }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  ragflow.conf.python: |
    server {
        listen 80;
        server_name _;
        root /ragflow/web/dist;

        gzip on;
        gzip_min_length 1k;
        gzip_comp_level 9;
        gzip_types text/plain application/javascript application/x-javascript text/css application/xml text/javascript application/x-httpd-php image/jpeg image/gif image/png;
        gzip_vary on;
        gzip_disable "MSIE [1-6]\.";

        location ~ ^/api/v1/admin {
            proxy_pass http://localhost:9381;
            include proxy.conf;
        }

        location ~ ^/(v1|api) {
            proxy_pass http://localhost:9380;
            include proxy.conf;
        }

        location / {
            index index.html;
            try_files $uri $uri/ /index.html;
        }

        # Cache-Control: max-age~@~AExpires
        location ~ ^/static/(css|js|media)/ {
            expires 10y;
            access_log off;
        }
    }
  proxy.conf: |
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_buffering off;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
  nginx.conf: |
    user  root;
    worker_processes  auto;

    error_log  /var/log/nginx/error.log notice;
    pid        /var/run/nginx.pid;

    events {
        worker_connections  1024;
    }

    http {
        include       /etc/nginx/mime.types;
        default_type  application/octet-stream;

        log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                        '$status $body_bytes_sent "$http_referer" '
                        '"$http_user_agent" "$http_x_forwarded_for"';

        access_log  /var/log/nginx/access.log  main;

        sendfile        on;
        #tcp_nopush     on;

        keepalive_timeout  65;

        #gzip  on;
        client_max_body_size 2048M;

        include /etc/nginx/conf.d/ragflow.conf;
    }
